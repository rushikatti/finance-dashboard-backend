services:
  - type: web
    name: finance-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python manage.py migrate && gunicorn backend_config.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9